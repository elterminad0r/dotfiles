# FIGMENTIZE: i3
# .__ ________
# |__|\_____  \
# |  |  _(__  <
# |  | /       \
# |__|/______  /
#            \/

# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.  #
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec urxvt -e sh -c "IZAAK_START_TMUX=true zsh"
# start terminal in cwd
bindsym $mod+Shift+Return exec urxvt -cd "`~/bin/xcwd`"

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym $mod+d exec --no-startup-id i3-dmenu-desktop --dmenu="$HOME/bin/dmenu_nice -p 'Desktop File'"

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# control the mouse with keyboard
bindsym $mod+Left exec xdotool mousemove_relative -- -100 0
bindsym $mod+Right exec xdotool mousemove_relative -- 100 0
bindsym $mod+Up exec xdotool mousemove_relative -- 0 -100
bindsym $mod+Down exec xdotool mousemove_relative -- -0 100

bindsym $mod+Shift+Left exec xdotool mousemove_relative -- -1 0
bindsym $mod+Shift+Right exec xdotool mousemove_relative -- 1 0
bindsym $mod+Shift+Up exec xdotool mousemove_relative -- 0 -1
bindsym $mod+Shift+Down exec xdotool mousemove_relative -- -0 1

bindsym $mod+slash exec xdotool click 1
bindsym $mod+Shift+slash exec xdotool click 3

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+q layout stacking
bindsym $mod+w layout tabbed
# both sets layout to tiling and changes "orientation" of split
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+a focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

bindsym $mod+grave exec --no-startup-id ~/bin/i3_empty_ws

# left/right between workspaces
# TODO: think of a better usage for minus and equal
bindsym $mod+minus workspace prev
bindsym $mod+equal workspace next
bindsym $mod+p workspace prev
bindsym $mod+n workspace next

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

bindsym $mod+Shift+grave exec --no-startup-id ~/bin/i3_empty_mv

# move container, and also move to workspace
bindsym $mod+F1 move container to workspace $ws1 ; workspace $ws1
bindsym $mod+F2 move container to workspace $ws2 ; workspace $ws2
bindsym $mod+F3 move container to workspace $ws3 ; workspace $ws3
bindsym $mod+F4 move container to workspace $ws4 ; workspace $ws4
bindsym $mod+F5 move container to workspace $ws5 ; workspace $ws5
bindsym $mod+F6 move container to workspace $ws6 ; workspace $ws6
bindsym $mod+F7 move container to workspace $ws7 ; workspace $ws7
bindsym $mod+F8 move container to workspace $ws8 ; workspace $ws8
bindsym $mod+F9 move container to workspace $ws9 ; workspace $ws9
bindsym $mod+F0 move container to workspace $ws10; workspace $ws10

# move focused container left/right
bindsym $mod+Shift+minus move container to workspace prev
bindsym $mod+Shift+equal move container to workspace next

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # And use Shift for small adjustments
        bindsym Shift+h resize shrink width 1 px or 1 ppt
        bindsym Shift+j resize grow height 1 px or 1 ppt
        bindsym Shift+k resize shrink height 1 px or 1 ppt
        bindsym Shift+l resize grow width 1 px or 1 ppt

        # allow holding mod key
        bindsym $mod+h resize shrink width 10 px or 10 ppt
        bindsym $mod+j resize grow height 10 px or 10 ppt
        bindsym $mod+k resize shrink height 10 px or 10 ppt
        bindsym $mod+l resize grow width 10 px or 10 ppt

        bindsym $mod+Shift+h resize shrink width 1 px or 1 ppt
        bindsym $mod+Shift+j resize grow height 1 px or 1 ppt
        bindsym $mod+Shift+k resize shrink height 1 px or 1 ppt
        bindsym $mod+Shift+l resize grow width 1 px or 1 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

exec_always --no-startup-id ~/.config/polybar/launch.sh
# a futile attempt to autohide polybar
# bindsym $mod exec --no-startup-id polybar-msg cmd show
# bindsym --release $mod exec --no-startup-id polybar-msg cmd hide

# # Start i3bar to display a workspace bar (plus the system information i3status
# # finds out, if available)
# bar {
#     font pango:DejaVu Sans Mono 12
#     mode hide
#     hidden_state hide
#     position top
#     status_command ~/bin/i3status_wrap # i3status # i3blocks #$HOME/bin/conky-i3bar
# }

# media bindings

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% && pkill -RTMIN+2 i3blocks #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% && pkill -RTMIN+2 i3blocks #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle && pkill -RTMIN+2 i3blocks # mute sound

# bindsym $mod+XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% && pkill -RTMIN+2 i3blocks #increase sound volume
# bindsym $mod+XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% && pkill -RTMIN+2 i3blocks #decrease sound volume
# bindsym $mod+XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle && pkill -RTMIN+2 i3blocks # mute sound

# finer grain with shift
bindsym Shift+XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +1% && pkill -RTMIN+2 i3blocks #increase sound volume
bindsym Shift+XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -1% && pkill -RTMIN+2 i3blocks #decrease sound volume

# bindsym $mod+Shift+XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +1% && pkill -RTMIN+2 i3blocks #increase sound volume
# bindsym $mod+Shift+XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -1% && pkill -RTMIN+2 i3blocks #decrease sound volume

# screen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 5 && pkill -RTMIN+3 i3blocks # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 5 && pkill -RTMIN+3 i3blocks # decrease screen brightness

# bindsym $mod+XF86MonBrightnessUp exec xbacklight -inc 5 && pkill -RTMIN+3 i3blocks # increase screen brightness
# bindsym $mod+XF86MonBrightnessDown exec xbacklight -dec 5 && pkill -RTMIN+3 i3blocks # decrease screen brightness

# finer grain with shift
bindsym Shift+XF86MonBrightnessUp exec xbacklight -inc 1 && pkill -RTMIN+3 i3blocks # increase screen brightness
bindsym Shift+XF86MonBrightnessDown exec xbacklight -dec 1 && pkill -RTMIN+3 i3blocks # decrease screen brightness

# bindsym $mod+Shift+XF86MonBrightnessUp exec xbacklight -inc 1 && pkill -RTMIN+3 i3blocks # increase screen brightness
# bindsym $mod+Shift+XF86MonBrightnessDown exec xbacklight -dec 1 && pkill -RTMIN+3 i3blocks # decrease screen brightness

# Media player controls
# bindsym XF86AudioPlay exec cmus-remote -u && pkill -RTMIN+1 i3blocks
# bindsym XF86AudioPause exec cmus-remote -u && pkill -RTMIN+1 i3blocks
# bindsym XF86AudioNext exec cmus-remote -n && pkill -RTMIN+1 i3blocks
# bindsym XF86AudioPrev exec cmus-remote -r && pkill -RTMIN+1 i3blocks

# bindsym $mod+XF86AudioPlay exec cmus-remote -u && pkill -RTMIN+1 i3blocks
# bindsym $mod+XF86AudioPause exec cmus-remote -u && pkill -RTMIN+1 i3blocks
# bindsym $mod+XF86AudioNext exec cmus-remote -n && pkill -RTMIN+1 i3blocks
# bindsym $mod+XF86AudioPrev exec cmus-remote -r && pkill -RTMIN+1 i3blocks

bindsym XF86AudioPlay exec mpc toggle -q
bindsym XF86AudioPause exec mpc toggle -q
bindsym XF86AudioNext exec mpc next -q
bindsym XF86AudioPrev exec mpc prev -q

bindsym $mod+XF86AudioMute exec --no-startup-id mpc repeat
bindsym $mod+XF86AudioLowerVolume exec --no-startup-id mpc random
bindsym $mod+XF86AudioRaiseVolume exec --no-startup-id mpc single

# bindsym $mod+XF86AudioPlay exec mpc toggle -q
# bindsym $mod+XF86AudioPause exec mpc toggle -q
# bindsym $mod+XF86AudioNext exec mpc next -q
# bindsym $mod+XF86AudioPrev exec mpc prev -q

# Application opening keybinds
bindsym $mod+bracketright exec firefox
bindsym $mod+bracketleft exec urxvt -e ncmpcpp -s visualizer
bindsym $mod+numbersign exec urxvt -e ipython --profile=maths
bindsym $mod+apostrophe exec evince "$(xclip -selection clipboard -o -)"

# hide mouse
bindsym $mod+m exec xdotool mousemove 1920 1080
bindsym $mod+Shift+m exec xdotool mousemove 960 540

# open lightdm greeter
bindsym $mod+g exec dm-tool switch-to-greeter

# TODO add sleep
bindsym --release $mod+Escape exec light-locker-command -l

# screenshotting
bindsym --release Print exec scrot '/home/izaak/Pictures/screenshots/full_screenshot_%Y%m%d_%H%M%S.png'
bindsym --release Shift+Print exec scrot -u '/home/izaak/Pictures/screenshots/win_screenshot_%Y%m%d_%H%M%S.png'
bindsym --release $mod+Shift+Print exec scrot -s '/home/izaak/Pictures/screenshots/sel_screenshot_%Y%m%d_%H%M%S.png'

bindsym $mod+period exec --no-startup-id ~/bin/randombg --random
bindsym $mod+Shift+comma exec --no-startup-id ~/bin/randombg --prev
bindsym $mod+comma exec --no-startup-id ~/bin/randombg --next

for_window [class=".*"] border pixel 0
# default_border pixel 1
# default_floating_border pixel 1

# smart_borders on

gaps inner 20
gaps outer 5
# smart_gaps on

exec --no-startup-id /usr/bin/dunst

exec --no-startup-id mpd
exec --no-startup-id redshift-gtk -l manual -l 52.2053:0.1218
exec --no-startup-id nm-applet
exec --no-startup-id light-locker
exec --no-startup-id ~/bin/randombg --random

exec urxvt #-e sh -c "IZAAK_START_TMUX=true zsh"
exec i3-msg "workspace $ws2; exec firefox"
# exec i3-msg "workspace $ws10; exec urxvt -e cmus"
exec i3-msg "workspace $ws1"
