#!/usr/bin/env sh

# Launch dmenu with nicer (and centralised) formatting options

# black magic: "$@" to expand to each given parameter as separate words.

dmenu -l 30 -fn "Liberation Mono-12" -nb "#222" -nf "#ddd" -sb "#555" -sf "#fff" -i "$@"
