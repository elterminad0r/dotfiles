" TeX filetype specific configuration overriding the TeX plugin

setlocal tabstop=1 softtabstop=-1 shiftwidth=0

" because I like my suffixes just the way they are and the plugin overwrites
" rather than appends
setlocal suffixesadd<
